import 'package:flutter/material.dart';

class Progressing extends StatelessWidget {
  const Progressing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('progressing'),
    );
  }
}
